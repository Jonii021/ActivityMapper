version: '3.9'

services:
  postgres:
    image: postgres:15
    container_name: efcore-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: efcore
      POSTGRES_PASSWORD: efcorepass
      POSTGRES_DB: appdb
    ports:
      - "5432:5432"
